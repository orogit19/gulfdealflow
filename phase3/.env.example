VITE_API_URL=http://localhost:8000
# Enable the operator Review workspace in this frontend deployment.
# Never put ADMIN_API_KEY in a VITE_* variable; Vite exposes those values to browsers.
VITE_ENABLE_ADMIN_UI=true
